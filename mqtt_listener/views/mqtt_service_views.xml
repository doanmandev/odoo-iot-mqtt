<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_mqtt_service_tree" model="ir.ui.view">
        <field name="name">mqtt.service.tree</field>
        <field name="model">mqtt.service</field>
        <field name="arch" type="xml">
            <tree string="MQTT Service">
                <field name="name" />
                <field name="last_start" />
                <field name="status" />
                <field name="connection_status" />
            </tree>
        </field>
    </record>

    <record id="view_mqtt_service_form" model="ir.ui.view">
        <field name="name">mqtt.service.form</field>
        <field name="model">mqtt.service</field>
        <field name="arch" type="xml">
            <form string="MQTT Service">
                <header>
                    <button name="start_mqtt_service" string="Khởi động Dịch vụ" type="object" class="oe_highlight"
                            attrs="{'invisible': [('status', '=', 'start')]}" />
                    <button name="stop_mqtt_service" string="Dừng Dịch vụ" type="object" 
                            attrs="{'invisible': [('status', '!=', 'start')]}" />
                </header>
                <sheet>
                    <group>
                        <field name="name" />
                        <field name="last_start" />
                        <field name="status" />
                        <field name="connection_status" />
                        <field name="thread_identifier" />
                        <field name="host_info" />
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_mqtt_service" model="ir.actions.act_window">
        <field name="name">MQTT Service</field>
        <field name="res_model">mqtt.service</field>
        <field name="view_mode">tree,form</field>
    </record>

    <menuitem id="menu_mqtt_service" 
              name="MQTT Service"
              parent="mqtt_integration.menu_mqtt_root"
              action="action_mqtt_service"
              sequence="40"/>
</odoo>